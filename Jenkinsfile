pipeline {
    agent any

    stages {
        stage('Check data') {
            steps {
                script {
                    sh ''' 
                    # Start the SSH agent and add the private key
	 	    ls
                    '''
                }
            }
        }
    }
}
